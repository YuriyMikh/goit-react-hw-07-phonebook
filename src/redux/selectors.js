export const selectContacts = state => state.contacts.items; //получаем необходимую часть состояния

export const selectFilteredData = state => state.filter.value; //получаем необходимую часть состояния
